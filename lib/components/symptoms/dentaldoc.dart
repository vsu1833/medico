import 'package:flutter/material.dart';

class Dentaldoc extends StatelessWidget {
  const Dentaldoc({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "Dental Doctors",
        ),
      ),
    );
  }
}